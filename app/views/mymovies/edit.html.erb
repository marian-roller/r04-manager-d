<div id="mymovies-edit-page" class="card mt-3 mb-5">
  <div class="d-flex justify-content-between align-items-center card-header">

    <div>
      <h1>Editing My movie</h1>
    </div>

    <div>
      <%= link_to 'Show', @mymovie, class: 'btn btn-primary btn-sm' %>
      <%= link_to 'Back', mymovies_path, class: 'btn btn-secondary btn-sm' %>
    </div>
  </div>

  <%= render 'form', mymovie: @mymovie %>
</div>
