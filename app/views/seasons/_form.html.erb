<%= form_with(model: season, local: true) do |form| %>
  <% if season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
        <% season.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.text_field :movie_id %>
  </div>

  <div class="field">
    <%= form.label :season_no %>
    <%= form.number_field :season_no %>
  </div>

  <div class="field">
    <%= form.label :episodes %>
    <%= form.number_field :episodes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
